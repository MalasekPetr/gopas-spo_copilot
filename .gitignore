# Claude working context — local only
CLAUDE.md
.claude/

# Vygenerovane credentials a lokalni vystupy — NIKDY necommitovat (public repo)
/scripts/student-credentials.csv
/scripts/*.local.*
*.local.*

# Model endpointy a klice — NIKDY necommitovat
.env
.env.*
appsettings.Development.json
appsettings.Local.json
**/.localConfigs
**/.localConfigs.*
env/.env.*.user

# Node build vystupy (solution/*.ts referencni reseni)
node_modules/
dist/
lib/
*.tsbuildinfo

# .NET (jen instruktorska dema: Agent Framework D3)
bin/
obj/
*.user
